# 项目开发约束

## 语言与时区

- 默认使用中文沟通、记录计划、交付说明和项目文档。
- 所有业务时间默认使用 `Asia/Shanghai` / `UTC+08:00`。
- 接口、任务、日志、导入导出和测试数据中的时间值必须显式说明时区或偏移；不要把 UTC/ISO 输出直接当业务展示时间。

## 微信小店接入

- 当前项目默认采用逐店 `appid/app_secret` 直连接入微信小店，不走服务商授权。
- 店铺密钥必须加密存储，禁止写入前端、日志、测试快照、示例数据或公开文档。
- 店铺 `app_secret`、`access_token` 等敏感值在 SQLite 中只能保存密文；主密钥优先进入系统 Keychain，Keychain 不可用时才使用本地加密兜底。
- 微信 token 默认优先使用稳定版凭据接口 `POST https://api.weixin.qq.com/cgi-bin/stable_token`，普通模式下不要频繁强制刷新。
- 店铺接入校验后必须同步店铺基础信息，并至少能查询关键接口的 API 额度；额度查询的 `cgi_path` 必须是以 `/` 开头的接口路径，不能包含 `https://api.weixin.qq.com` 域名。
- 微信 API 调用必须通过统一 client 封装，统一处理 `access_token` 缓存、刷新、限流、重试、错误码归一化和请求日志。
- 微信接口日志只能保存接口、店铺、状态、错误码、错误摘要和脱敏响应摘要，禁止保存 `secret`、`access_token`、完整收件信息。
- 官方文档缓存放在 `docs/wechat-shop-api/`；实现接口前先查本地 `index.md`、`implementation-notes.md` 和对应官方源链接。

## 业务与队列

- 系统定位为合规外部供应商代发与运营中台，不实现刷单、虚假动销、绕审核或规避平台规则能力。
- 本地主控 HTTP API 默认只允许监听 `127.0.0.1`，不得默认暴露到局域网或公网。
- 外部 HTTP API 必须启用 API Key 鉴权；API Key 只能通过桌面端生成/重置，禁止写入日志、文档样例、测试快照或前端固定常量。
- 外部 HTTP API 请求审计只能保存方法、路径、状态码、耗时、错误码和脱敏摘要；禁止保存 API Key、完整请求体、完整商品详情或任何密钥。
- 外部系统创建铺货、改价等任务后只拿 `task_id` 并轮询查询状态；不得在 HTTP 请求里同步等待微信长耗时队列完成。
- 外部系统或后续 agent/skill 优先复用 `scripts/wx_xd_local_api.py` 调用本地主控 API；新增命令必须保持零依赖、API Key 不打印、请求摘要不包含密钥或收件人敏感信息。
- 采购相关本地 HTTP API 只允许查询非敏采购任务、回填供应商物流和标记供应商异常；不得暴露收件人姓名、手机号、地址，不得自动向供应商平台下单。
- 采购相关本地 HTTP API 审计只记录任务 ID、异常类型、是否有物流单号/备注等脱敏摘要；禁止保存完整物流请求体、收件信息或供应商平台登录凭证。
- 供应商下单 agent/skill 优先复用 `scripts/wx_xd_local_api.py` 或等价本地 HTTP API 协议；不得直接读写 SQLite 或绕过 API Key 鉴权、审计和自动发货开关。
- 供应商售后协同只能通过 `record_supplier_aftersale_followup` 或等价本地 HTTP API 记录脱敏沟通摘要、索证状态和可选采购任务引用；不得保存收件信息、供应商平台登录凭证、Cookie、完整聊天截图或自动处理平台纠纷。
- 供应商 agent 安全桥优先使用 `scripts/wx_xd_supplier_agent.py` 和 `docs/supplier-agent-protocol.md`；只允许导出非敏采购字段，并只写回 `shipment`、`issue`、`mapping` 三类结果。
- 桌面端供应商 agent 桥必须复用 `export_supplier_agent_tasks`、`get_supplier_agent_result_template`、`apply_supplier_agent_results`；不得在前端自行解析并绕过后端字段校验。
- 供应商 agent/skill 的结果文件如包含收件信息、密钥、Cookie、供应商平台凭证或未知字段，必须拒绝处理；不得为了自动化便利放宽字段校验。
- 订单同步、订单详情同步、采购任务生成和微信发货提交可以通过本地 runner API 单独触发，但仍必须走队列任务、API Key 鉴权、审计日志和自动发货开关；不得把这些步骤合并成无状态的同步 HTTP 请求。
- 订单、发货、售后、库存、铺货和动销数据同步必须通过队列任务执行，不能在页面请求里长时间阻塞。
- AI provider 默认关闭；只支持用户在桌面端显式配置 OpenAI-compatible `base_url/model/API Key` 后启用。
- AI provider API Key 必须按店铺密钥同等级加密存储，禁止写入前端固定值、日志、文档样例、测试快照、错误堆栈或外部 API 审计。
- AI 属性补齐只能基于商品标题、SKU、外部元数据、微信类目详情缓存和官方规则生成建议；不允许用 AI 绕过微信审核、伪造资质、虚构品牌授权或规避平台规则。
- 人工采纳 `publish_attribute_suggestions` 只能写回发品草稿并回到 `ready_to_publish` 或继续保持 `CATEGORY_ATTRS_NEED_AI_FILL`；采纳动作不得跳过微信 `categoryprecheck`、素材上传、`addproduct` 或审核状态同步。
- 铺货任务详情展示属性建议时必须按当前 `job_id`/`item_id` 过滤，避免运营误采纳其他任务或其他店铺的建议。
- 自动推进能力只能串联现有队列 runner，不得绕过单步任务状态机直接改最终状态；单个步骤失败时必须记录 `step/error` 并继续后续步骤。
- 自动推进必须按铺货状态机顺序执行 `publish.precheck_products`、`publish.fill_required_attributes`、`publish.category_precheck`、`publish.upload_assets`、`publish.submit_products`、`publish.sync_status`、`publish.listing_products`；各步骤可关闭，但不能合并跳过中间态。
- 自动推进可在类目预检前执行 `publish.fill_required_attributes`；启用 AI provider 时可先执行 `publish.ai_attribute_suggestions`，再应用外部 `metadata.ai_attr_suggestions`、AI 建议、微信类目单一可选值、SKU 规格同义词和明确标题规则；无法高置信判断时必须只写入 `publish_attribute_suggestions` 和 prompt，不得臆造属性值。
- 自动推进里的微信发货步骤仍必须尊重 `delivery.auto_send_delivery` 开关；开关关闭时不得调用微信 `senddelivery`。
- 订单同步首阶段只同步订单列表和订单号，默认拉取微信待发货状态 `20`；订单详情和收件人敏感信息必须单独走受控流程。
- 订单列表同步不得保存收件人姓名、手机号、地址等敏感信息；后续如需解密必须记录操作者、原因、订单和时间。
- 订单详情同步可以落订单项、商品 ID、SKU ID、外部商品/外部 SKU 映射、数量和金额；原始订单详情必须白名单脱敏保存，禁止保存收件人姓名、手机号、地址等履约敏感字段。
- 售后同步必须走 `aftersales.sync_shop_aftersales` 队列任务；售后详情入库前必须递归脱敏。当前售后可做人工责任归因、处理备注、供应商赔付回款记录，以及人工触发的同意/拒绝动作，但不自动上传售后凭证。
- 本地凭证资料包只能通过人工操作或受控本地 API `record_aftersale_evidence` 记录售后/纠纷的凭证元数据，通过 `update_aftersale_evidence_status` 更新本地整理状态；只能保存凭证类型、标题、脱敏说明、本地文件路径和来源链接，不得读取文件内容、上传微信、提交平台处理或保存收件人敏感信息。
- 售后/纠纷列表里的凭证数量只能来自 `aftersale_evidence_records` 本地元数据，用于内部资料整理进度，不代表微信侧已上传凭证或平台已处理。
- 本地凭证状态 `draft/ready/used/archived` 只代表内部整理进度，`used` 也不等于已向微信上传凭证或已完成平台举证；未来若接入平台处理动作，必须另做受控命令、开关和审计。
- 本地凭证资料包导出只能通过 `export_aftersale_evidence` 输出本地元数据清单，导出文件不得包含收件人姓名、手机号、地址、文件二进制内容、微信 `access_token`、店铺密钥或平台处理结果；导出动作不等同于微信举证。
- 本地凭证资料包的 HTTP API 审计只能记录目标类型、目标是否存在、凭证类型、状态以及是否有说明/文件/来源链接；状态更新审计只能记录凭证 ID 是否存在和目标状态，不得保存完整说明、本地文件路径、来源链接或任何图片内容。
- 供应商售后协同记录只代表内部沟通进度，不计入利润、不调用微信或供应商平台；供应商赔付回款仍必须通过售后责任归因、纠纷跟进或订单级利润调整形成可追踪记录。
- 售后同意/拒绝必须由人工或受控本地主控 API 明确触发，不能进入自动推进 runner；每次调用必须记录 `last_action`、`last_action_status`、`last_action_error`、`last_action_note`、`last_action_at`，并写入微信 API 日志和通知中心。
- 售后责任归因和订单利润调整可由人工或受控本地主控 API 记录；审计摘要只能包含售后 ID、订单 ID、责任方、调整类型、金额和是否有备注，不得保存完整售后详情、凭证、收件信息或供应商平台凭证。
- 供应商赔付回款必须通过售后责任归因或订单级利润调整形成可追踪记录，不能只写页面临时备注或绕过利润核算表。
- 售后拒绝原因类型必须优先来自按店铺同步的微信官方 `rejectreason/get` 缓存；如缓存缺失仍允许人工输入，但不得在代码里硬编码固定业务原因替代官方字典。
- 微信纠纷/保障单同步必须走 `aftersales.sync_guarantee_orders` 队列任务；详情入库前必须递归脱敏，待商家处理或举证状态必须写入通知中心。当前只同步、展示和本地跟进，不自动处理、不上传凭证、不进入自动推进 runner。
- 纠纷本地跟进只能通过人工操作或受控本地 API `record_guarantee_followup` 记录 `handling_status`、责任方、备注和供应商赔付；该动作不得调用微信纠纷处理接口、不得上传凭证、不得伪造平台处理结果。
- 纠纷供应商赔付回款必须在纠纷已关联本地订单时写入订单级 `other_income` 调整项；未关联订单时只能保留在纠纷本地记录上，并在通知中心提示人工后续核对。
- 微信售后列表单次同步窗口不得超过官方限制的 24 小时；单个店铺失败不能阻塞其他店铺，单个售后详情失败必须在“售后异常”里保留 `sync_failed` 和原因。
- 铺货失败、采购缺映射、采购供应商异常、同单多物流、发货失败、订单详情同步失败、售后待处理和售后同步失败必须写入 `notifications`，通知正文只能保存脱敏摘要和来源 ID。
- 采购任务从订单项生成，优先使用店铺 SKU 与外部商品 ID/外部 SKU 的映射；缺少映射时进入 `needs_mapping` 并通知人工处理，不能静默跳过。
- 外部同步商品必须保存可回溯的货源链接 `source_url`，并在 `publish_products`、`shop_products`、`purchase_tasks`、采购表和供应商 agent 导出中带出；链接不得包含登录态、Cookie、token、密钥、收件信息或一次性私密参数。
- 采购任务映射补齐只能通过人工操作或受控本地 API `resolve_purchase_task_mapping` 执行；必须同时更新 `order_items` 与 `purchase_tasks`，清除缺映射通知，不得读取或写入收件人姓名、手机号、地址。
- 采购任务列表和采购表导出默认只包含订单号、商品、SKU、数量、金额、映射、货源链接和处理原因；不得导出收件人姓名、手机号、地址。
- 首版采购为人工处理，不自动调用外部平台下单；自动采购 agent/skill 后续必须做成独立适配器和可关闭能力。
- 采购任务物流回填只能记录供应商物流和成本；同一微信订单下所有采购任务都回填且物流信息一致时，才允许自动生成订单级微信发货单。
- 同一微信订单存在多个供应商物流时，不得自动整单发货，必须进入人工确认或后续实现多包裹发货能力。
- 物流单号首版人工录入；微信发货可自动执行，但必须由配置开关控制，关闭时只进入“待确认发货”。
- 履约发货本地 API 可以查询发货队列、同步快递公司、录入物流、重试失败发货和切换自动发货开关；审计摘要不得保存完整物流请求体、收件信息或供应商平台凭证，自动发货关闭时禁止提交微信发货。
- 自寄快递发货前优先按店铺同步微信 `getdeliverycompanylistnew` 快递公司列表；`delivery_id` 必须来自官方列表或明确兜底为 `OTHER`，不得臆造快递公司编码。
- 微信发货必须走 `delivery.submit_wechat_shipment` 队列任务；自动发货开关关闭时禁止调用微信 `senddelivery`。
- 发货失败必须在履约队列中可见，重试只能清除本地错误并重新进入待确认/待提交状态，不得绕过自动发货开关直接调用微信。
- 调用微信 `senddelivery` 前必须用本地 `order_items` 构造 `product_infos`，商品 ID、SKU ID 或数量缺失时进入失败，不得猜测发货商品。
- 供应商缺货、涨价、取消和质量风险只能通过 `mark_purchase_task_issue` 标记为采购异常并通知人工处理，不自动换供应商、不自动取消采购或订单。
- 订单利润必须核算预估毛利和实际毛利，至少包含成交价、数量、采购成本、采购运费、退款、售后赔付、供应商赔付回款和其他成本。
- 实际毛利只有在订单采购任务存在且采购成本补齐后才算确定；采购运费、退款、售后赔付、供应商赔付回款和其他成本必须以订单级调整项记录，不能只在页面临时计算。
- 库存更新优先使用增减量语义；谨慎使用直接设置库存，避免并发覆盖。
- 库存风控只能基于外部商品 SKU 库存、采购占用、供应商异常和店铺商品映射生成低库存/断货提醒；不得虚构库存、制造虚假动销或绕过真实库存约束。
- 库存风险扫描必须走 `inventory.scan_risks` 队列任务并写入通知中心，不能在页面里做不可追踪的临时判断。
- 商品动销分析必须以真实订单、真实采购成本、真实售后、真实库存和店铺商品映射为依据；运营建议只能用于铺货、调价、补货、观察或下架决策，不得触发刷单、虚假交易或虚假动销。
- 批量改价必须任务化执行；未真实调用微信 `updateproduct` 前最多只能进入 `ready_to_update`，禁止把本地校验通过伪造成已改价成功。
- 提交微信 `updateproduct` 前必须先用 `getproduct` 获取商品线上/草稿数据，只允许在完整商品参数基础上修改 `skus[].sale_price`；不得只构造价格片段提交。
- `updateproduct` 返回成功只能进入 `submitted`，表示微信已接收改价请求；后续必须通过商品状态/价格同步确认后，才能标记最终成功。
- 改价最终成功只能以微信 `getproduct(data_type=3)` 返回的线上 `product.skus[].sale_price` 全部等于目标价为准；如果只有 `edit_product` 草稿价格匹配，必须保持 `audit_pending` 继续轮询，不得标记成功。
- 改价任务必须按店铺商品维度记录目标价、微信商品 ID、状态和失败原因；缺少已铺货商品或微信 `product_id` 时必须失败可见。
- 对微信小店接口的失败调用必须保留店铺、接口、任务、错误码、响应摘要和重试状态，方便排障和补偿。
- 铺货任务本地前置校验通过只能进入 `ready_to_publish`；后续类目预检和素材上传可推进到 `category_prechecked`、`assets_ready`，但禁止把这些中间态伪造成发布成功。
- 微信图片素材上传完成后只能进入 `assets_ready`；真实 `addproduct` 成功并拿到微信商品 ID 后只能进入 `submitted`。
- 微信商品图片必须使用素材接口返回的 `mmecimage.cn/p/` 链接；外部图片 URL 不得直接传给 `addproduct`。
- 外部商品图片进入微信前必须先做本地素材预处理：下载图片、禁止 301/302 跳转、校验格式/大小/宽高，必要时压缩或转 JPEG，再走微信 `img/upload` 二进制上传。
- `addproduct` 提交成功后先进入 `submitted`，保存微信 `product_id`；审核状态未同步前不要标记为最终成功。
- `submitted` 和 `audit_pending` 必须通过微信 `getproduct` 轮询审核/商品状态；`status=5` 才是已上架成功，`status=4` 或 `edit_status=4` 只能算 `audit_passed`，后续仍需上架动作。
- 只有 `audit_passed` 的任务项允许调用微信 `listingproduct`；上架接口成功后必须回到 `audit_pending`，再用 `getproduct` 确认 `status=5`，不能把接口返回 ok 直接当已上架。
- 不要对审核中、上传中或异步提审中的商品重复调用 `listingproduct`，避免触发微信限频或封禁。
- 微信商品状态 `3/8/10/13/14/15/20/21/30/71/72/73` 或编辑状态 `3/8/72/73` 必须沉淀为任务项失败原因，方便按商品和店铺维度重试。
- 自动发品提交必须先有可验证的微信发品参数：优先使用外部传入的 `metadata.wechat_add_product_payload`；没有完整 payload 时，系统只能基于 `metadata.wechat_category_ids`、`wechat_attrs`、SKU、定价、运费和服务配置生成草稿，禁止臆造真实类目 ID 或必填属性。
- 生成发品草稿时，`head_imgs` 和 `desc_info.imgs` 仍必须由素材上传阶段注入；外部原图 URL 不能直接进入微信 `addproduct`。
- 微信类目树、类目详情、商品发布规则、发货方式规则和运费模板必须按店铺缓存；发品参数补齐和 AI 推荐优先查本地缓存，缓存缺失时先同步官方接口，不能用过期猜测替代真实规则。
- 素材上传前必须优先执行微信 `categoryprecheck`；本地类目详情缓存用于检查必填商品属性/销售属性，官方预检用于检查店铺发品权限、类目可用性、运费险和保证金等资质问题。
- `CATEGORY_ATTRS_NEED_AI_FILL` 失败项必须优先走 `run_publish_attribute_fill_once`；自动补齐成功只能回到 `ready_to_publish` 重新执行类目预检，不能直接跳到素材上传或发布成功。
- 低置信或来源不确定的属性建议必须进入人工确认界面；批量采纳后仍要重新跑 `run_publish_category_prechecks_once`。
- 批量铺货必须按任务项推进，单个店铺失败只影响该任务项，必须能从任务中心和商品详情看到失败原因。

## 系统设计决策

- 系统先按 Tauri v2 桌面端优先设计，面向两人内部使用；默认不建设独立 Web 服务端。
- 默认技术栈为 `Tauri v2 + React 18 + TypeScript + Vite + Rust + SQLite`，Rust 侧承载微信 API client、本地任务、加密、文件和数据库能力；前端用自研响应式 shim（`src/runtime/reactive.ts`）承接原 composable 状态，UI 为「Soft」设计方向。
- 系统仍按模块化单体设计，模块边界清晰但不拆微服务；跨模块异步工作通过本地任务表和 Rust 后台 worker 解耦。
- 默认不引入 `PostgreSQL`、`Redis`、`Celery`、`FastAPI`；只有明确需要多人在线协作或服务器无人值守任务时才升级为 Web/服务端架构。
- 核心商品模型固定为“供应商商品 -> 标准商品 -> 店铺商品”，不要把供应商商品或微信商品直接当作唯一业务商品。
- 店铺维度必须贯穿权限、任务、调用日志、库存、订单、售后和经营数据，所有批量操作都要能按店铺隔离、暂停、重试和追踪。
- 动销能力只做真实订单、真实流量、真实库存和真实经营数据分析，输出选品、铺货、调价、下架、补货建议。
- 本地 SQLite 数据库必须支持一键备份、自动备份、恢复校验和后续迁移到服务端数据库。
- 恢复数据库前必须先自动创建 `pre-restore` 回滚备份，并对来源备份和恢复后数据库执行 `PRAGMA integrity_check`；禁止从应用备份目录外的任意路径直接覆盖数据库。
- 应用启动自动备份必须按上海日期控制每天最多一次，并复用 WAL checkpoint、SHA-256 和 SQLite 完整性校验；自动备份失败只记录错误，不阻断桌面端启动。
